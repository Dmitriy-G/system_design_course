1. API layer should only depend on service layer
2. Communications layer should only depend on communications
3. Infrastructure layer must depend only infrastructure
4. Validation must not depend on service layer
5. Order module must not depend on specific module services
6. Notification module must not depend on specific module services
7. Payment module must not depend on specific module services
8. Customer module must not depend on specific module services